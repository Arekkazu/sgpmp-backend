info:
  name: INICIAR SESION
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8000/sesiones
  body:
    type: json
    data: |-
      {
        "correo_electronico": "arek3071@gmail.com",
        "contrasena": "MiPasswoddrd123#"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
