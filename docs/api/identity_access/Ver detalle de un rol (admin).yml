info:
  name: " Ver detalle de un rol (admin)"
  type: http
  seq: 18

http:
  method: get
  url: http://localhost:8000/roles/
  headers:
    - name: Authorization
      value: Bearer <TOKEN>
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
