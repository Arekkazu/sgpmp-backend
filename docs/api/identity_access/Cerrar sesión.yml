info:
  name: Cerrar sesión
  type: http
  seq: 3

http:
  method: delete
  url: http://localhost:8000/sesiones/
  headers:
    - name: Authorization
      value: Bearer <TOKEN>
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
