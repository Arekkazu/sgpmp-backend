info:
  name: " Eliminar rol (como admin)"
  type: http
  seq: 20

http:
  method: delete
  url: http://localhost:8000/roles/
  headers:
    - name: Authorization
      value: Bearer <TOKEN>
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
