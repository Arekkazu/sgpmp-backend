info:
  name: Listar usuarios paginado (admin)
  type: http
  seq: 9

http:
  method: get
  url: http://localhost:8000/usuarios/admin?pagina=1&tamano=20
  headers:
    - name: Authorization
      value: Bearer <TOKEN>
  params:
    - name: pagina
      value: "1"
      type: query
    - name: tamano
      value: "20"
      type: query
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
