info:
  name: "  Caso 9 — Formato inválido → 422"
  type: http
  seq: 10

http:
  method: post
  url: http://localhost:8000/contrasena/recuperar
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "correo_electronico": "no-es-un-correo"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
