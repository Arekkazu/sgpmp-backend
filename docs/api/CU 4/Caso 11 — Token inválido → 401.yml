info:
  name: " Caso 11 — Token inválido → 401"
  type: http
  seq: 12

http:
  method: post
  url: http://localhost:8000/contrasena/restablecer
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "token": "token_inventado_invalido",
        "nueva_contrasena": "NuevaClave1@",
        "confirmar_contrasena": "NuevaClave1@"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
