info:
  name: Caso 2 — Contraseña actual incorrecta → 401
  type: http
  seq: 3

http:
  method: put
  url: http://localhost:8000/contrasena/usuarios/1
  headers:
    - name: Authorization
      value: Bearer {TOKEN}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "contrasena_actual": "contrasena_incorrecta",
        "nueva_contrasena": "NuevaClave1@",
        "confirmar_nueva_contrasena": "NuevaClave1@"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
