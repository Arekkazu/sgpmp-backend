info:
  name: Caso 5 — Política no cumplida → 422
  type: http
  seq: 6

http:
  method: put
  url: http://localhost:8000/contrasena/usuarios/1
  headers:
    - name: Authorization
      value: Bearer {TOKEN}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "contrasena_actual": "tu_password_actual",
        "nueva_contrasena": "debil",
        "confirmar_nueva_contrasena": "debil"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
