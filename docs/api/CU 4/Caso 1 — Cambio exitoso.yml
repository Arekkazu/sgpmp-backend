info:
  name: Caso 1 — Cambio exitoso
  type: http
  seq: 2

http:
  method: put
  url: http://localhost:8000/contrasena/usuarios/1
  headers:
    - name: Authorization
      value: Bearer {TOKEN}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "contrasena_actual": "tu_password_actual",
        "nueva_contrasena": "NuevaClave1@",
        "confirmar_nueva_contrasena": "NuevaClave1@"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
