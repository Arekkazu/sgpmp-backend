{
  "info": {
    "name": "TC-M02-G83 - RF-48 existencia de activo, origen y destino",
    "description": "RF-48 / TC-M02-G83. Cuatro variantes sobre POST /activos-biologicos/{id_activo}/transferencias: TC-M02-306 (E-02 activo inexistente), TC-M02-307 (E-04 activo ACTIVO sin infraestructura origen), TC-M02-308-A (E-05 destino inexistente) y TC-M02-308-B (E-05 destino existente pero INACTIVO), ejecutadas con Productor y Administrador: 8 peticiones oficiales. Cada peticion aisla una unica causa de rechazo sobre un payload base valido. El SETUP es de solo lectura.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "00-SETUP-LECTURA",
      "item": [
        {
          "name": "Contrato vivo OpenAPI por HTTPS",
          "request": {
            "method": "GET",
            "header": [],
            "url": "https://sigab-backendtest-389pcb-a48238-158-69-200-27.sslip.io/api-sgpmp-test/openapi.json"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const norm = (s) => String(s).normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').toLowerCase();",
                  "const cuerpo = () => { try { return JSON.stringify(pm.response.json()); } catch (e) { return pm.response.text(); } };",
                  "pm.test('El ambiente TEST HTTPS responde 200', () => pm.response.to.have.status(200));",
                  "const spec = pm.response.json();",
                  "const ruta = '/activos-biologicos/{id_activo}/transferencias';",
                  "pm.test('El contrato declara POST ' + ruta, () => pm.expect(spec.paths).to.have.property(ruta));",
                  "const resp = Object.keys(spec.paths[ruta].post.responses);",
                  "pm.collectionVariables.set('respuestas_declaradas', resp.join(','));",
                  "pm.test('El contrato declara 404, exigido por TC-M02-306', () => pm.expect(resp).to.include('404'));",
                  "pm.test('El contrato declara 422, exigido por TC-M02-307 y TC-M02-308', () => pm.expect(resp).to.include('422'));",
                  "const dto = spec.components.schemas.RegistrarTransferenciaDTO;",
                  "pm.test('El campo de destino se llama infraestructura_destino_id', () => pm.expect(dto.properties).to.have.property('infraestructura_destino_id'));",
                  "pm.test('fecha_transferencia se declara con formato date', () => pm.expect(dto.properties.fecha_transferencia.format).to.eql('date'));"
                ]
              }
            }
          ]
        },
        {
          "name": "Login (lectura de sesion) - Productor",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": "https://sigab-backendtest-389pcb-a48238-158-69-200-27.sslip.io/api-sgpmp-test/sesiones/",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"correo_electronico\": \"m2m.nuevo@ejemplo.com\",\n  \"contrasena\": \"{{password}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const norm = (s) => String(s).normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').toLowerCase();",
                  "const cuerpo = () => { try { return JSON.stringify(pm.response.json()); } catch (e) { return pm.response.text(); } };",
                  "pm.test('Login exitoso - Productor', () => pm.response.to.have.status(200));",
                  "const t = pm.response.json().token;",
                  "pm.collectionVariables.set('token_productor', t);",
                  "const sub = JSON.parse(atob(t.split('.')[1])).sub;",
                  "pm.test('El token pertenece al usuario 35 (Productor)', () => pm.expect(String(sub)).to.eql('35'));"
                ]
              }
            }
          ]
        },
        {
          "name": "Login (lectura de sesion) - Administrador",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": "https://sigab-backendtest-389pcb-a48238-158-69-200-27.sslip.io/api-sgpmp-test/sesiones/",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"correo_electronico\": \"admin@pecuaria.co\",\n  \"contrasena\": \"{{password}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const norm = (s) => String(s).normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').toLowerCase();",
                  "const cuerpo = () => { try { return JSON.stringify(pm.response.json()); } catch (e) { return pm.response.text(); } };",
                  "pm.test('Login exitoso - Administrador', () => pm.response.to.have.status(200));",
                  "const t = pm.response.json().token;",
                  "pm.collectionVariables.set('token_admin', t);",
                  "const sub = JSON.parse(atob(t.split('.')[1])).sub;",
                  "pm.test('El token pertenece al usuario 1 (Administrador)', () => pm.expect(String(sub)).to.eql('1'));"
                ]
              }
            }
          ]
        },
        {
          "name": "Precondicion TC-M02-306: el activo 99999 no existe",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token_productor}}"
              }
            ],
            "url": "https://sigab-backendtest-389pcb-a48238-158-69-200-27.sslip.io/api-sgpmp-test/activos-biologicos/99999"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const norm = (s) => String(s).normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').toLowerCase();",
                  "const cuerpo = () => { try { return JSON.stringify(pm.response.json()); } catch (e) { return pm.response.text(); } };",
                  "pm.test('El activo 99999 no existe: la consulta devuelve 404', () => pm.response.to.have.status(404));",
                  "pm.test('El 404 es de negocio (ACTIVO_NO_ENCONTRADO), no de ruta inexistente', () => pm.expect(pm.response.json().error_code).to.eql('ACTIVO_NO_ENCONTRADO'));",
                  "pm.collectionVariables.set('activo_inexistente_confirmado', 'SI');"
                ]
              }
            }
          ]
        },
        {
          "name": "Precondicion TC-M02-307: activo 287 ACTIVO y sin origen vigente",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token_productor}}"
              }
            ],
            "url": "https://sigab-backendtest-389pcb-a48238-158-69-200-27.sslip.io/api-sgpmp-test/activos-biologicos/287"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const norm = (s) => String(s).normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').toLowerCase();",
                  "const cuerpo = () => { try { return JSON.stringify(pm.response.json()); } catch (e) { return pm.response.text(); } };",
                  "pm.test('Acceso legitimo al activo 287', () => pm.response.to.have.status(200));",
                  "const b = pm.response.json();",
                  "pm.test('El activo 287 existe y es QAJE-TRF-SINORIG', () => pm.expect(b.identificador).to.eql('QAJE-TRF-SINORIG'));",
                  "pm.test('El activo 287 esta ACTIVO', () => pm.expect(b.nombre_estado).to.eql('ACTIVO'));",
                  "pm.collectionVariables.set('estado_287', b.nombre_estado);"
                ]
              }
            }
          ]
        },
        {
          "name": "Precondicion TC-M02-307: el activo 287 no registra ubicacion previa",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token_productor}}"
              }
            ],
            "url": "https://sigab-backendtest-389pcb-a48238-158-69-200-27.sslip.io/api-sgpmp-test/activos-biologicos/287/historial?page_size=100"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const norm = (s) => String(s).normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').toLowerCase();",
                  "const cuerpo = () => { try { return JSON.stringify(pm.response.json()); } catch (e) { return pm.response.text(); } };",
                  "pm.test('Historial legible del activo 287', () => pm.response.to.have.status(200));",
                  "const infra = pm.response.json().registros.filter((r) => r.categoria === 'INFRAESTRUCTURA');",
                  "pm.test('El activo 287 no tiene ningun registro de infraestructura', () => pm.expect(infra.length).to.eql(0));",
                  "pm.collectionVariables.set('sin_origen_confirmado', 'SI');"
                ]
              }
            }
          ]
        },
        {
          "name": "Precondicion TC-M02-308: activo 279 ACTIVO con origen 48",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token_productor}}"
              }
            ],
            "url": "https://sigab-backendtest-389pcb-a48238-158-69-200-27.sslip.io/api-sgpmp-test/activos-biologicos/279"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const norm = (s) => String(s).normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').toLowerCase();",
                  "const cuerpo = () => { try { return JSON.stringify(pm.response.json()); } catch (e) { return pm.response.text(); } };",
                  "pm.test('Acceso legitimo al activo 279', () => pm.response.to.have.status(200));",
                  "const b = pm.response.json();",
                  "pm.test('El activo 279 esta ACTIVO', () => pm.expect(b.nombre_estado).to.eql('ACTIVO'));",
                  "pm.test('El activo 279 tiene como origen vigente la infraestructura 48', () => pm.expect(b.id_infraestructura).to.eql(48));",
                  "pm.collectionVariables.set('origen_279', b.id_infraestructura);"
                ]
              }
            }
          ]
        },
        {
          "name": "Precondicion TC-M02-308-B: el destino 50 existe pero esta INACTIVO",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token_productor}}"
              }
            ],
            "url": "https://sigab-backendtest-389pcb-a48238-158-69-200-27.sslip.io/api-sgpmp-test/activos-biologicos/279/transferencias/disponibles"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const norm = (s) => String(s).normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').toLowerCase();",
                  "const cuerpo = () => { try { return JSON.stringify(pm.response.json()); } catch (e) { return pm.response.text(); } };",
                  "pm.test('El endpoint de destinos disponibles responde 200', () => pm.response.to.have.status(200));",
                  "const porId = Object.fromEntries(pm.response.json().map((i) => [i.id_infraestructura, i]));",
                  "pm.test('El destino INACTIVO 50 no figura entre los destinos disponibles', () => pm.expect(porId).to.not.have.property('50'));",
                  "pm.test('El destino inexistente 99999 tampoco figura', () => pm.expect(porId).to.not.have.property('99999'));",
                  "pm.test('El destino valido 51 si figura, con Corral, especie 40 y capacidad 200', () => {",
                  "  pm.expect(porId[51], 'destino 51 ausente').to.be.an('object');",
                  "  pm.expect(porId[51].tipo).to.eql('Corral');",
                  "  pm.expect(porId[51].id_especie).to.eql(40);",
                  "  pm.expect(porId[51].capacidad_maxima).to.eql(200);",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "01-CASO-PRINCIPAL",
      "item": [
        {
          "name": "Productor",
          "item": [
            {
              "name": "TC-M02-306 - activo 99999 inexistente - Productor",
              "request": {
                "method": "POST",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{token_productor}}"
                  },
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  }
                ],
                "url": "https://sigab-backendtest-389pcb-a48238-158-69-200-27.sslip.io/api-sgpmp-test/activos-biologicos/99999/transferencias",
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"infraestructura_origen_id\": 48,\n  \"infraestructura_destino_id\": 51,\n  \"fecha_transferencia\": \"{{fecha_hoy}}\",\n  \"motivo_transferencia\": \"TC-M02-306 Productor: verificacion de RF-48\"\n}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                }
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const norm = (s) => String(s).normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').toLowerCase();",
                      "const cuerpo = () => { try { return JSON.stringify(pm.response.json()); } catch (e) { return pm.response.text(); } };",
                      "const enviado = JSON.parse(pm.request.body.raw);",
                      "const code = pm.response.code;",
                      "const b = (() => { try { return pm.response.json(); } catch (e) { return {}; } })();",
                      "const txt = norm(cuerpo());",
                      "pm.test('TC-M02-306 / Productor: la peticion viaja autenticada', () => pm.expect(String(pm.request.headers.get('Authorization'))).to.match(/^Bearer .+/));",
                      "pm.test('TC-M02-306 / Productor: el resto del payload es valido (origen real y activo, fecha no futura, motivo presente)', () => {",
                      "  pm.expect(enviado.infraestructura_origen_id).to.eql(48);",
                      "  pm.expect(enviado.infraestructura_destino_id).to.eql(51);",
                      "  pm.expect(enviado.infraestructura_destino_id).to.not.eql(enviado.infraestructura_origen_id);",
                      "  pm.expect(enviado.fecha_transferencia).to.match(/^\\d{4}-\\d{2}-\\d{2}$/);",
                      "  pm.expect(new Date(enviado.fecha_transferencia + 'T00:00:00Z').getTime()).to.be.at.most(Date.now());",
                      "  pm.expect(String(enviado.motivo_transferencia).trim()).to.not.eql('');",
                      "});",
                      "pm.test('TC-M02-306 / Productor: la peticion es rechazada, no se crea la transferencia', () => pm.expect(code).to.not.be.within(200, 299));",
                      "pm.test('TC-M02-306 / Productor: el actor autorizado no recibe 403', () => pm.expect(code).to.not.eql(403));",
                      "pm.test('TC-M02-306 / Productor: no se produce un error de servidor', () => pm.expect(code).to.be.below(500));",
                      "pm.test('TC-M02-306 / Productor: el activo 99999 fue confirmado inexistente antes del POST', () => pm.expect(pm.collectionVariables.get('activo_inexistente_confirmado')).to.eql('SI'));",
                      "pm.test('TC-M02-306 / Productor: la URL apunta realmente al activo 99999', () => pm.expect(pm.request.url.toString()).to.include('/activos-biologicos/99999/transferencias'));",
                      "pm.test('TC-M02-306 / Productor: HTTP 404 segun la ficha (E-02)', () => pm.expect(code).to.eql(404));",
                      "pm.test('TC-M02-306 / Productor: el error corresponde a E-02 (ACTIVO_NO_ENCONTRADO)', () => pm.expect(b.error_code).to.eql('ACTIVO_NO_ENCONTRADO'));",
                      "pm.test('TC-M02-306 / Productor: el mensaje corresponde a activo inexistente y nombra el ID', () => {",
                      "  pm.expect(txt).to.match(/no fue encontrado|no existe/);",
                      "  pm.expect(txt).to.include('99999');",
                      "});",
                      "pm.test('TC-M02-306 / Productor: el 404 no proviene de una ruta inexistente', () => {",
                      "  pm.expect(b, 'un 404 de enrutado responderia {detail: Not Found}').to.not.have.property('detail');",
                      "  pm.expect(b).to.have.property('error_code');",
                      "});",
                      "pm.test('TC-M02-306 / Productor: el rechazo no se debe a otra regla de RF-48', () => { ['sin_infraestructura_origen', 'infraestructura_origen_incorrecta', 'infraestructura_destino_invalida', 'destino_igual_origen', 'incompatibilidad_especie', 'capacidad_excedida', 'activo_no_activo', 'transferencia_concurrente'].forEach((otro) => { pm.expect(txt, 'rechazo atribuido a ' + otro).to.not.include(otro); }); });",
                      "pm.collectionVariables.set('http_productor_306', code);",
                      "pm.collectionVariables.set('body_productor_306', pm.response.text().slice(0, 600));"
                    ]
                  }
                }
              ]
            },
            {
              "name": "Verificacion tras TC-M02-306: el activo 99999 sigue sin existir - Productor",
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{token_productor}}"
                  }
                ],
                "url": "https://sigab-backendtest-389pcb-a48238-158-69-200-27.sslip.io/api-sgpmp-test/activos-biologicos/99999"
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const norm = (s) => String(s).normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').toLowerCase();",
                      "const cuerpo = () => { try { return JSON.stringify(pm.response.json()); } catch (e) { return pm.response.text(); } };",
                      "pm.test('TC-M02-306 / Productor: el POST rechazado no creo el activo 99999', () => pm.response.to.have.status(404));",
                      "pm.test('TC-M02-306 / Productor: sigue reportandose como inexistente', () => pm.expect(pm.response.json().error_code).to.eql('ACTIVO_NO_ENCONTRADO'));"
                    ]
                  }
                }
              ]
            },
            {
              "name": "TC-M02-307 - activo sin infraestructura origen - Productor",
              "request": {
                "method": "POST",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{token_productor}}"
                  },
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  }
                ],
                "url": "https://sigab-backendtest-389pcb-a48238-158-69-200-27.sslip.io/api-sgpmp-test/activos-biologicos/287/transferencias",
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"infraestructura_origen_id\": 48,\n  \"infraestructura_destino_id\": 51,\n  \"fecha_transferencia\": \"{{fecha_hoy}}\",\n  \"motivo_transferencia\": \"TC-M02-307 Productor: verificacion de RF-48\"\n}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                }
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const norm = (s) => String(s).normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').toLowerCase();",
                      "const cuerpo = () => { try { return JSON.stringify(pm.response.json()); } catch (e) { return pm.response.text(); } };",
                      "const enviado = JSON.parse(pm.request.body.raw);",
                      "const code = pm.response.code;",
                      "const b = (() => { try { return pm.response.json(); } catch (e) { return {}; } })();",
                      "const txt = norm(cuerpo());",
                      "pm.test('TC-M02-307 / Productor: la peticion viaja autenticada', () => pm.expect(String(pm.request.headers.get('Authorization'))).to.match(/^Bearer .+/));",
                      "pm.test('TC-M02-307 / Productor: el resto del payload es valido (origen real y activo, fecha no futura, motivo presente)', () => {",
                      "  pm.expect(enviado.infraestructura_origen_id).to.eql(48);",
                      "  pm.expect(enviado.infraestructura_destino_id).to.eql(51);",
                      "  pm.expect(enviado.infraestructura_destino_id).to.not.eql(enviado.infraestructura_origen_id);",
                      "  pm.expect(enviado.fecha_transferencia).to.match(/^\\d{4}-\\d{2}-\\d{2}$/);",
                      "  pm.expect(new Date(enviado.fecha_transferencia + 'T00:00:00Z').getTime()).to.be.at.most(Date.now());",
                      "  pm.expect(String(enviado.motivo_transferencia).trim()).to.not.eql('');",
                      "});",
                      "pm.test('TC-M02-307 / Productor: la peticion es rechazada, no se crea la transferencia', () => pm.expect(code).to.not.be.within(200, 299));",
                      "pm.test('TC-M02-307 / Productor: el actor autorizado no recibe 403', () => pm.expect(code).to.not.eql(403));",
                      "pm.test('TC-M02-307 / Productor: no se produce un error de servidor', () => pm.expect(code).to.be.below(500));",
                      "pm.test('TC-M02-307 / Productor: el activo 287 existe y esta ACTIVO', () => pm.expect(pm.collectionVariables.get('estado_287')).to.eql('ACTIVO'));",
                      "pm.test('TC-M02-307 / Productor: se confirmo que no tiene origen vigente antes del POST', () => pm.expect(pm.collectionVariables.get('sin_origen_confirmado')).to.eql('SI'));",
                      "pm.test('TC-M02-307 / Productor: HTTP 422 segun la ficha (E-04)', () => pm.expect(code).to.eql(422));",
                      "pm.test('TC-M02-307 / Productor: el error corresponde a E-04 (SIN_INFRAESTRUCTURA_ORIGEN)', () => pm.expect(b.error_code).to.eql('SIN_INFRAESTRUCTURA_ORIGEN'));",
                      "pm.test('TC-M02-307 / Productor: el mensaje identifica la ausencia de infraestructura origen', () => {",
                      "  pm.expect(txt).to.include('infraestructura origen');",
                      "  pm.expect(txt).to.match(/no tiene|asocie/);",
                      "});",
                      "pm.test('TC-M02-307 / Productor: el rechazo no se debe a otra regla de RF-48', () => { ['activo_no_encontrado', 'infraestructura_origen_incorrecta', 'infraestructura_destino_invalida', 'destino_igual_origen', 'incompatibilidad_especie', 'capacidad_excedida', 'activo_no_activo', 'transferencia_concurrente'].forEach((otro) => { pm.expect(txt, 'rechazo atribuido a ' + otro).to.not.include(otro); }); });",
                      "pm.collectionVariables.set('http_productor_307', code);",
                      "pm.collectionVariables.set('body_productor_307', pm.response.text().slice(0, 600));"
                    ]
                  }
                }
              ]
            },
            {
              "name": "Verificacion sin cambios tras TC-M02-307 - Productor",
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{token_productor}}"
                  }
                ],
                "url": "https://sigab-backendtest-389pcb-a48238-158-69-200-27.sslip.io/api-sgpmp-test/activos-biologicos/287"
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const norm = (s) => String(s).normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').toLowerCase();",
                      "const cuerpo = () => { try { return JSON.stringify(pm.response.json()); } catch (e) { return pm.response.text(); } };",
                      "pm.test('Activo legible', () => pm.response.to.have.status(200));",
                      "const b = pm.response.json();",
                      "pm.test('TC-M02-307 / Productor: el activo 287 sigue ACTIVO', () => pm.expect(b.nombre_estado).to.eql('ACTIVO'));",
                      "pm.test('TC-M02-307 / Productor: la ubicacion del activo 287 no cambio (48)', () => pm.expect(b.id_infraestructura).to.eql(48));"
                    ]
                  }
                }
              ]
            },
            {
              "name": "TC-M02-308-A - destino 99999 inexistente - Productor",
              "request": {
                "method": "POST",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{token_productor}}"
                  },
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  }
                ],
                "url": "https://sigab-backendtest-389pcb-a48238-158-69-200-27.sslip.io/api-sgpmp-test/activos-biologicos/279/transferencias",
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"infraestructura_origen_id\": 48,\n  \"infraestructura_destino_id\": 99999,\n  \"fecha_transferencia\": \"{{fecha_hoy}}\",\n  \"motivo_transferencia\": \"TC-M02-308-A Productor: verificacion de RF-48\"\n}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                }
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const norm = (s) => String(s).normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').toLowerCase();",
                      "const cuerpo = () => { try { return JSON.stringify(pm.response.json()); } catch (e) { return pm.response.text(); } };",
                      "const enviado = JSON.parse(pm.request.body.raw);",
                      "const code = pm.response.code;",
                      "const b = (() => { try { return pm.response.json(); } catch (e) { return {}; } })();",
                      "const txt = norm(cuerpo());",
                      "pm.test('TC-M02-308-A / Productor: la peticion viaja autenticada', () => pm.expect(String(pm.request.headers.get('Authorization'))).to.match(/^Bearer .+/));",
                      "pm.test('TC-M02-308-A / Productor: el resto del payload es valido (origen real y activo, fecha no futura, motivo presente)', () => {",
                      "  pm.expect(enviado.infraestructura_origen_id).to.eql(48);",
                      "  pm.expect(enviado.infraestructura_destino_id).to.eql(99999);",
                      "  pm.expect(enviado.infraestructura_destino_id).to.not.eql(enviado.infraestructura_origen_id);",
                      "  pm.expect(enviado.fecha_transferencia).to.match(/^\\d{4}-\\d{2}-\\d{2}$/);",
                      "  pm.expect(new Date(enviado.fecha_transferencia + 'T00:00:00Z').getTime()).to.be.at.most(Date.now());",
                      "  pm.expect(String(enviado.motivo_transferencia).trim()).to.not.eql('');",
                      "});",
                      "pm.test('TC-M02-308-A / Productor: la peticion es rechazada, no se crea la transferencia', () => pm.expect(code).to.not.be.within(200, 299));",
                      "pm.test('TC-M02-308-A / Productor: el actor autorizado no recibe 403', () => pm.expect(code).to.not.eql(403));",
                      "pm.test('TC-M02-308-A / Productor: no se produce un error de servidor', () => pm.expect(code).to.be.below(500));",
                      "pm.test('TC-M02-308-A / Productor: el activo base 279 esta ACTIVO y su origen vigente es 48', () => pm.expect(Number(pm.collectionVariables.get('origen_279'))).to.eql(48));",
                      "pm.test('TC-M02-308-A / Productor: HTTP 422 segun la ficha (E-05)', () => pm.expect(code).to.eql(422));",
                      "pm.test('TC-M02-308-A / Productor: el error corresponde a E-05 (INFRAESTRUCTURA_DESTINO_INVALIDA)', () => pm.expect(b.error_code).to.eql('INFRAESTRUCTURA_DESTINO_INVALIDA'));",
                      "pm.test('TC-M02-308-A / Productor: el mensaje corresponde a destino inexistente o inactivo', () => {",
                      "  pm.expect(txt).to.match(/no existe|no esta activa/);",
                      "  pm.expect(txt).to.include('99999');",
                      "});",
                      "pm.test('TC-M02-308-A / Productor: el rechazo no se debe a otra regla de RF-48', () => { ['activo_no_encontrado', 'sin_infraestructura_origen', 'infraestructura_origen_incorrecta', 'destino_igual_origen', 'incompatibilidad_especie', 'capacidad_excedida', 'activo_no_activo', 'transferencia_concurrente'].forEach((otro) => { pm.expect(txt, 'rechazo atribuido a ' + otro).to.not.include(otro); }); });",
                      "pm.collectionVariables.set('http_productor_308a', code);",
                      "pm.collectionVariables.set('body_productor_308a', pm.response.text().slice(0, 600));"
                    ]
                  }
                }
              ]
            },
            {
              "name": "Verificacion sin cambios tras TC-M02-308-A - Productor",
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{token_productor}}"
                  }
                ],
                "url": "https://sigab-backendtest-389pcb-a48238-158-69-200-27.sslip.io/api-sgpmp-test/activos-biologicos/279"
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const norm = (s) => String(s).normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').toLowerCase();",
                      "const cuerpo = () => { try { return JSON.stringify(pm.response.json()); } catch (e) { return pm.response.text(); } };",
                      "pm.test('Activo legible', () => pm.response.to.have.status(200));",
                      "const b = pm.response.json();",
                      "pm.test('TC-M02-308-A / Productor: el activo 279 sigue ACTIVO', () => pm.expect(b.nombre_estado).to.eql('ACTIVO'));",
                      "pm.test('TC-M02-308-A / Productor: la ubicacion del activo 279 no cambio (48)', () => pm.expect(b.id_infraestructura).to.eql(48));"
                    ]
                  }
                }
              ]
            },
            {
              "name": "TC-M02-308-B - destino 50 INACTIVO - Productor",
              "request": {
                "method": "POST",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{token_productor}}"
                  },
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  }
                ],
                "url": "https://sigab-backendtest-389pcb-a48238-158-69-200-27.sslip.io/api-sgpmp-test/activos-biologicos/279/transferencias",
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"infraestructura_origen_id\": 48,\n  \"infraestructura_destino_id\": 50,\n  \"fecha_transferencia\": \"{{fecha_hoy}}\",\n  \"motivo_transferencia\": \"TC-M02-308-B Productor: verificacion de RF-48\"\n}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                }
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const norm = (s) => String(s).normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').toLowerCase();",
                      "const cuerpo = () => { try { return JSON.stringify(pm.response.json()); } catch (e) { return pm.response.text(); } };",
                      "const enviado = JSON.parse(pm.request.body.raw);",
                      "const code = pm.response.code;",
                      "const b = (() => { try { return pm.response.json(); } catch (e) { return {}; } })();",
                      "const txt = norm(cuerpo());",
                      "pm.test('TC-M02-308-B / Productor: la peticion viaja autenticada', () => pm.expect(String(pm.request.headers.get('Authorization'))).to.match(/^Bearer .+/));",
                      "pm.test('TC-M02-308-B / Productor: el resto del payload es valido (origen real y activo, fecha no futura, motivo presente)', () => {",
                      "  pm.expect(enviado.infraestructura_origen_id).to.eql(48);",
                      "  pm.expect(enviado.infraestructura_destino_id).to.eql(50);",
                      "  pm.expect(enviado.infraestructura_destino_id).to.not.eql(enviado.infraestructura_origen_id);",
                      "  pm.expect(enviado.fecha_transferencia).to.match(/^\\d{4}-\\d{2}-\\d{2}$/);",
                      "  pm.expect(new Date(enviado.fecha_transferencia + 'T00:00:00Z').getTime()).to.be.at.most(Date.now());",
                      "  pm.expect(String(enviado.motivo_transferencia).trim()).to.not.eql('');",
                      "});",
                      "pm.test('TC-M02-308-B / Productor: la peticion es rechazada, no se crea la transferencia', () => pm.expect(code).to.not.be.within(200, 299));",
                      "pm.test('TC-M02-308-B / Productor: el actor autorizado no recibe 403', () => pm.expect(code).to.not.eql(403));",
                      "pm.test('TC-M02-308-B / Productor: no se produce un error de servidor', () => pm.expect(code).to.be.below(500));",
                      "pm.test('TC-M02-308-B / Productor: el activo base 279 esta ACTIVO y su origen vigente es 48', () => pm.expect(Number(pm.collectionVariables.get('origen_279'))).to.eql(48));",
                      "pm.test('TC-M02-308-B / Productor: el destino 50 es distinto del origen', () => pm.expect(enviado.infraestructura_destino_id).to.not.eql(48));",
                      "pm.test('TC-M02-308-B / Productor: HTTP 422 segun la ficha (E-05)', () => pm.expect(code).to.eql(422));",
                      "pm.test('TC-M02-308-B / Productor: el error corresponde a E-05 (INFRAESTRUCTURA_DESTINO_INVALIDA)', () => pm.expect(b.error_code).to.eql('INFRAESTRUCTURA_DESTINO_INVALIDA'));",
                      "pm.test('TC-M02-308-B / Productor: el mensaje corresponde a destino no activo', () => {",
                      "  pm.expect(txt).to.match(/no existe|no esta activa/);",
                      "  pm.expect(txt).to.include('50');",
                      "});",
                      "pm.test('TC-M02-308-B / Productor: el rechazo no se debe a otra regla de RF-48', () => { ['activo_no_encontrado', 'sin_infraestructura_origen', 'infraestructura_origen_incorrecta', 'destino_igual_origen', 'incompatibilidad_especie', 'capacidad_excedida', 'activo_no_activo', 'transferencia_concurrente'].forEach((otro) => { pm.expect(txt, 'rechazo atribuido a ' + otro).to.not.include(otro); }); });",
                      "pm.collectionVariables.set('http_productor_308b', code);",
                      "pm.collectionVariables.set('body_productor_308b', pm.response.text().slice(0, 600));"
                    ]
                  }
                }
              ]
            },
            {
              "name": "Verificacion sin cambios tras TC-M02-308-B - Productor",
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{token_productor}}"
                  }
                ],
                "url": "https://sigab-backendtest-389pcb-a48238-158-69-200-27.sslip.io/api-sgpmp-test/activos-biologicos/279"
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const norm = (s) => String(s).normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').toLowerCase();",
                      "const cuerpo = () => { try { return JSON.stringify(pm.response.json()); } catch (e) { return pm.response.text(); } };",
                      "pm.test('Activo legible', () => pm.response.to.have.status(200));",
                      "const b = pm.response.json();",
                      "pm.test('TC-M02-308-B / Productor: el activo 279 sigue ACTIVO', () => pm.expect(b.nombre_estado).to.eql('ACTIVO'));",
                      "pm.test('TC-M02-308-B / Productor: la ubicacion del activo 279 no cambio (48)', () => pm.expect(b.id_infraestructura).to.eql(48));"
                    ]
                  }
                }
              ]
            }
          ]
        },
        {
          "name": "Administrador",
          "item": [
            {
              "name": "TC-M02-306 - activo 99999 inexistente - Administrador",
              "request": {
                "method": "POST",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{token_admin}}"
                  },
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  }
                ],
                "url": "https://sigab-backendtest-389pcb-a48238-158-69-200-27.sslip.io/api-sgpmp-test/activos-biologicos/99999/transferencias",
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"infraestructura_origen_id\": 48,\n  \"infraestructura_destino_id\": 51,\n  \"fecha_transferencia\": \"{{fecha_hoy}}\",\n  \"motivo_transferencia\": \"TC-M02-306 Administrador: verificacion de RF-48\"\n}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                }
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const norm = (s) => String(s).normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').toLowerCase();",
                      "const cuerpo = () => { try { return JSON.stringify(pm.response.json()); } catch (e) { return pm.response.text(); } };",
                      "const enviado = JSON.parse(pm.request.body.raw);",
                      "const code = pm.response.code;",
                      "const b = (() => { try { return pm.response.json(); } catch (e) { return {}; } })();",
                      "const txt = norm(cuerpo());",
                      "pm.test('TC-M02-306 / Administrador: la peticion viaja autenticada', () => pm.expect(String(pm.request.headers.get('Authorization'))).to.match(/^Bearer .+/));",
                      "pm.test('TC-M02-306 / Administrador: el resto del payload es valido (origen real y activo, fecha no futura, motivo presente)', () => {",
                      "  pm.expect(enviado.infraestructura_origen_id).to.eql(48);",
                      "  pm.expect(enviado.infraestructura_destino_id).to.eql(51);",
                      "  pm.expect(enviado.infraestructura_destino_id).to.not.eql(enviado.infraestructura_origen_id);",
                      "  pm.expect(enviado.fecha_transferencia).to.match(/^\\d{4}-\\d{2}-\\d{2}$/);",
                      "  pm.expect(new Date(enviado.fecha_transferencia + 'T00:00:00Z').getTime()).to.be.at.most(Date.now());",
                      "  pm.expect(String(enviado.motivo_transferencia).trim()).to.not.eql('');",
                      "});",
                      "pm.test('TC-M02-306 / Administrador: la peticion es rechazada, no se crea la transferencia', () => pm.expect(code).to.not.be.within(200, 299));",
                      "pm.test('TC-M02-306 / Administrador: el actor autorizado no recibe 403', () => pm.expect(code).to.not.eql(403));",
                      "pm.test('TC-M02-306 / Administrador: no se produce un error de servidor', () => pm.expect(code).to.be.below(500));",
                      "pm.test('TC-M02-306 / Administrador: el activo 99999 fue confirmado inexistente antes del POST', () => pm.expect(pm.collectionVariables.get('activo_inexistente_confirmado')).to.eql('SI'));",
                      "pm.test('TC-M02-306 / Administrador: la URL apunta realmente al activo 99999', () => pm.expect(pm.request.url.toString()).to.include('/activos-biologicos/99999/transferencias'));",
                      "pm.test('TC-M02-306 / Administrador: HTTP 404 segun la ficha (E-02)', () => pm.expect(code).to.eql(404));",
                      "pm.test('TC-M02-306 / Administrador: el error corresponde a E-02 (ACTIVO_NO_ENCONTRADO)', () => pm.expect(b.error_code).to.eql('ACTIVO_NO_ENCONTRADO'));",
                      "pm.test('TC-M02-306 / Administrador: el mensaje corresponde a activo inexistente y nombra el ID', () => {",
                      "  pm.expect(txt).to.match(/no fue encontrado|no existe/);",
                      "  pm.expect(txt).to.include('99999');",
                      "});",
                      "pm.test('TC-M02-306 / Administrador: el 404 no proviene de una ruta inexistente', () => {",
                      "  pm.expect(b, 'un 404 de enrutado responderia {detail: Not Found}').to.not.have.property('detail');",
                      "  pm.expect(b).to.have.property('error_code');",
                      "});",
                      "pm.test('TC-M02-306 / Administrador: el rechazo no se debe a otra regla de RF-48', () => { ['sin_infraestructura_origen', 'infraestructura_origen_incorrecta', 'infraestructura_destino_invalida', 'destino_igual_origen', 'incompatibilidad_especie', 'capacidad_excedida', 'activo_no_activo', 'transferencia_concurrente'].forEach((otro) => { pm.expect(txt, 'rechazo atribuido a ' + otro).to.not.include(otro); }); });",
                      "pm.collectionVariables.set('http_admin_306', code);",
                      "pm.collectionVariables.set('body_admin_306', pm.response.text().slice(0, 600));"
                    ]
                  }
                }
              ]
            },
            {
              "name": "Verificacion tras TC-M02-306: el activo 99999 sigue sin existir - Administrador",
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{token_admin}}"
                  }
                ],
                "url": "https://sigab-backendtest-389pcb-a48238-158-69-200-27.sslip.io/api-sgpmp-test/activos-biologicos/99999"
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const norm = (s) => String(s).normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').toLowerCase();",
                      "const cuerpo = () => { try { return JSON.stringify(pm.response.json()); } catch (e) { return pm.response.text(); } };",
                      "pm.test('TC-M02-306 / Administrador: el POST rechazado no creo el activo 99999', () => pm.response.to.have.status(404));",
                      "pm.test('TC-M02-306 / Administrador: sigue reportandose como inexistente', () => pm.expect(pm.response.json().error_code).to.eql('ACTIVO_NO_ENCONTRADO'));"
                    ]
                  }
                }
              ]
            },
            {
              "name": "TC-M02-307 - activo sin infraestructura origen - Administrador",
              "request": {
                "method": "POST",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{token_admin}}"
                  },
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  }
                ],
                "url": "https://sigab-backendtest-389pcb-a48238-158-69-200-27.sslip.io/api-sgpmp-test/activos-biologicos/287/transferencias",
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"infraestructura_origen_id\": 48,\n  \"infraestructura_destino_id\": 51,\n  \"fecha_transferencia\": \"{{fecha_hoy}}\",\n  \"motivo_transferencia\": \"TC-M02-307 Administrador: verificacion de RF-48\"\n}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                }
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const norm = (s) => String(s).normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').toLowerCase();",
                      "const cuerpo = () => { try { return JSON.stringify(pm.response.json()); } catch (e) { return pm.response.text(); } };",
                      "const enviado = JSON.parse(pm.request.body.raw);",
                      "const code = pm.response.code;",
                      "const b = (() => { try { return pm.response.json(); } catch (e) { return {}; } })();",
                      "const txt = norm(cuerpo());",
                      "pm.test('TC-M02-307 / Administrador: la peticion viaja autenticada', () => pm.expect(String(pm.request.headers.get('Authorization'))).to.match(/^Bearer .+/));",
                      "pm.test('TC-M02-307 / Administrador: el resto del payload es valido (origen real y activo, fecha no futura, motivo presente)', () => {",
                      "  pm.expect(enviado.infraestructura_origen_id).to.eql(48);",
                      "  pm.expect(enviado.infraestructura_destino_id).to.eql(51);",
                      "  pm.expect(enviado.infraestructura_destino_id).to.not.eql(enviado.infraestructura_origen_id);",
                      "  pm.expect(enviado.fecha_transferencia).to.match(/^\\d{4}-\\d{2}-\\d{2}$/);",
                      "  pm.expect(new Date(enviado.fecha_transferencia + 'T00:00:00Z').getTime()).to.be.at.most(Date.now());",
                      "  pm.expect(String(enviado.motivo_transferencia).trim()).to.not.eql('');",
                      "});",
                      "pm.test('TC-M02-307 / Administrador: la peticion es rechazada, no se crea la transferencia', () => pm.expect(code).to.not.be.within(200, 299));",
                      "pm.test('TC-M02-307 / Administrador: el actor autorizado no recibe 403', () => pm.expect(code).to.not.eql(403));",
                      "pm.test('TC-M02-307 / Administrador: no se produce un error de servidor', () => pm.expect(code).to.be.below(500));",
                      "pm.test('TC-M02-307 / Administrador: el activo 287 existe y esta ACTIVO', () => pm.expect(pm.collectionVariables.get('estado_287')).to.eql('ACTIVO'));",
                      "pm.test('TC-M02-307 / Administrador: se confirmo que no tiene origen vigente antes del POST', () => pm.expect(pm.collectionVariables.get('sin_origen_confirmado')).to.eql('SI'));",
                      "pm.test('TC-M02-307 / Administrador: HTTP 422 segun la ficha (E-04)', () => pm.expect(code).to.eql(422));",
                      "pm.test('TC-M02-307 / Administrador: el error corresponde a E-04 (SIN_INFRAESTRUCTURA_ORIGEN)', () => pm.expect(b.error_code).to.eql('SIN_INFRAESTRUCTURA_ORIGEN'));",
                      "pm.test('TC-M02-307 / Administrador: el mensaje identifica la ausencia de infraestructura origen', () => {",
                      "  pm.expect(txt).to.include('infraestructura origen');",
                      "  pm.expect(txt).to.match(/no tiene|asocie/);",
                      "});",
                      "pm.test('TC-M02-307 / Administrador: el rechazo no se debe a otra regla de RF-48', () => { ['activo_no_encontrado', 'infraestructura_origen_incorrecta', 'infraestructura_destino_invalida', 'destino_igual_origen', 'incompatibilidad_especie', 'capacidad_excedida', 'activo_no_activo', 'transferencia_concurrente'].forEach((otro) => { pm.expect(txt, 'rechazo atribuido a ' + otro).to.not.include(otro); }); });",
                      "pm.collectionVariables.set('http_admin_307', code);",
                      "pm.collectionVariables.set('body_admin_307', pm.response.text().slice(0, 600));"
                    ]
                  }
                }
              ]
            },
            {
              "name": "Verificacion sin cambios tras TC-M02-307 - Administrador",
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{token_admin}}"
                  }
                ],
                "url": "https://sigab-backendtest-389pcb-a48238-158-69-200-27.sslip.io/api-sgpmp-test/activos-biologicos/287"
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const norm = (s) => String(s).normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').toLowerCase();",
                      "const cuerpo = () => { try { return JSON.stringify(pm.response.json()); } catch (e) { return pm.response.text(); } };",
                      "pm.test('Activo legible', () => pm.response.to.have.status(200));",
                      "const b = pm.response.json();",
                      "pm.test('TC-M02-307 / Administrador: el activo 287 sigue ACTIVO', () => pm.expect(b.nombre_estado).to.eql('ACTIVO'));",
                      "pm.test('TC-M02-307 / Administrador: la ubicacion del activo 287 no cambio (48)', () => pm.expect(b.id_infraestructura).to.eql(48));"
                    ]
                  }
                }
              ]
            },
            {
              "name": "TC-M02-308-A - destino 99999 inexistente - Administrador",
              "request": {
                "method": "POST",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{token_admin}}"
                  },
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  }
                ],
                "url": "https://sigab-backendtest-389pcb-a48238-158-69-200-27.sslip.io/api-sgpmp-test/activos-biologicos/279/transferencias",
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"infraestructura_origen_id\": 48,\n  \"infraestructura_destino_id\": 99999,\n  \"fecha_transferencia\": \"{{fecha_hoy}}\",\n  \"motivo_transferencia\": \"TC-M02-308-A Administrador: verificacion de RF-48\"\n}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                }
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const norm = (s) => String(s).normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').toLowerCase();",
                      "const cuerpo = () => { try { return JSON.stringify(pm.response.json()); } catch (e) { return pm.response.text(); } };",
                      "const enviado = JSON.parse(pm.request.body.raw);",
                      "const code = pm.response.code;",
                      "const b = (() => { try { return pm.response.json(); } catch (e) { return {}; } })();",
                      "const txt = norm(cuerpo());",
                      "pm.test('TC-M02-308-A / Administrador: la peticion viaja autenticada', () => pm.expect(String(pm.request.headers.get('Authorization'))).to.match(/^Bearer .+/));",
                      "pm.test('TC-M02-308-A / Administrador: el resto del payload es valido (origen real y activo, fecha no futura, motivo presente)', () => {",
                      "  pm.expect(enviado.infraestructura_origen_id).to.eql(48);",
                      "  pm.expect(enviado.infraestructura_destino_id).to.eql(99999);",
                      "  pm.expect(enviado.infraestructura_destino_id).to.not.eql(enviado.infraestructura_origen_id);",
                      "  pm.expect(enviado.fecha_transferencia).to.match(/^\\d{4}-\\d{2}-\\d{2}$/);",
                      "  pm.expect(new Date(enviado.fecha_transferencia + 'T00:00:00Z').getTime()).to.be.at.most(Date.now());",
                      "  pm.expect(String(enviado.motivo_transferencia).trim()).to.not.eql('');",
                      "});",
                      "pm.test('TC-M02-308-A / Administrador: la peticion es rechazada, no se crea la transferencia', () => pm.expect(code).to.not.be.within(200, 299));",
                      "pm.test('TC-M02-308-A / Administrador: el actor autorizado no recibe 403', () => pm.expect(code).to.not.eql(403));",
                      "pm.test('TC-M02-308-A / Administrador: no se produce un error de servidor', () => pm.expect(code).to.be.below(500));",
                      "pm.test('TC-M02-308-A / Administrador: el activo base 279 esta ACTIVO y su origen vigente es 48', () => pm.expect(Number(pm.collectionVariables.get('origen_279'))).to.eql(48));",
                      "pm.test('TC-M02-308-A / Administrador: HTTP 422 segun la ficha (E-05)', () => pm.expect(code).to.eql(422));",
                      "pm.test('TC-M02-308-A / Administrador: el error corresponde a E-05 (INFRAESTRUCTURA_DESTINO_INVALIDA)', () => pm.expect(b.error_code).to.eql('INFRAESTRUCTURA_DESTINO_INVALIDA'));",
                      "pm.test('TC-M02-308-A / Administrador: el mensaje corresponde a destino inexistente o inactivo', () => {",
                      "  pm.expect(txt).to.match(/no existe|no esta activa/);",
                      "  pm.expect(txt).to.include('99999');",
                      "});",
                      "pm.test('TC-M02-308-A / Administrador: el rechazo no se debe a otra regla de RF-48', () => { ['activo_no_encontrado', 'sin_infraestructura_origen', 'infraestructura_origen_incorrecta', 'destino_igual_origen', 'incompatibilidad_especie', 'capacidad_excedida', 'activo_no_activo', 'transferencia_concurrente'].forEach((otro) => { pm.expect(txt, 'rechazo atribuido a ' + otro).to.not.include(otro); }); });",
                      "pm.collectionVariables.set('http_admin_308a', code);",
                      "pm.collectionVariables.set('body_admin_308a', pm.response.text().slice(0, 600));"
                    ]
                  }
                }
              ]
            },
            {
              "name": "Verificacion sin cambios tras TC-M02-308-A - Administrador",
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{token_admin}}"
                  }
                ],
                "url": "https://sigab-backendtest-389pcb-a48238-158-69-200-27.sslip.io/api-sgpmp-test/activos-biologicos/279"
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const norm = (s) => String(s).normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').toLowerCase();",
                      "const cuerpo = () => { try { return JSON.stringify(pm.response.json()); } catch (e) { return pm.response.text(); } };",
                      "pm.test('Activo legible', () => pm.response.to.have.status(200));",
                      "const b = pm.response.json();",
                      "pm.test('TC-M02-308-A / Administrador: el activo 279 sigue ACTIVO', () => pm.expect(b.nombre_estado).to.eql('ACTIVO'));",
                      "pm.test('TC-M02-308-A / Administrador: la ubicacion del activo 279 no cambio (48)', () => pm.expect(b.id_infraestructura).to.eql(48));"
                    ]
                  }
                }
              ]
            },
            {
              "name": "TC-M02-308-B - destino 50 INACTIVO - Administrador",
              "request": {
                "method": "POST",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{token_admin}}"
                  },
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  }
                ],
                "url": "https://sigab-backendtest-389pcb-a48238-158-69-200-27.sslip.io/api-sgpmp-test/activos-biologicos/279/transferencias",
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"infraestructura_origen_id\": 48,\n  \"infraestructura_destino_id\": 50,\n  \"fecha_transferencia\": \"{{fecha_hoy}}\",\n  \"motivo_transferencia\": \"TC-M02-308-B Administrador: verificacion de RF-48\"\n}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                }
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const norm = (s) => String(s).normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').toLowerCase();",
                      "const cuerpo = () => { try { return JSON.stringify(pm.response.json()); } catch (e) { return pm.response.text(); } };",
                      "const enviado = JSON.parse(pm.request.body.raw);",
                      "const code = pm.response.code;",
                      "const b = (() => { try { return pm.response.json(); } catch (e) { return {}; } })();",
                      "const txt = norm(cuerpo());",
                      "pm.test('TC-M02-308-B / Administrador: la peticion viaja autenticada', () => pm.expect(String(pm.request.headers.get('Authorization'))).to.match(/^Bearer .+/));",
                      "pm.test('TC-M02-308-B / Administrador: el resto del payload es valido (origen real y activo, fecha no futura, motivo presente)', () => {",
                      "  pm.expect(enviado.infraestructura_origen_id).to.eql(48);",
                      "  pm.expect(enviado.infraestructura_destino_id).to.eql(50);",
                      "  pm.expect(enviado.infraestructura_destino_id).to.not.eql(enviado.infraestructura_origen_id);",
                      "  pm.expect(enviado.fecha_transferencia).to.match(/^\\d{4}-\\d{2}-\\d{2}$/);",
                      "  pm.expect(new Date(enviado.fecha_transferencia + 'T00:00:00Z').getTime()).to.be.at.most(Date.now());",
                      "  pm.expect(String(enviado.motivo_transferencia).trim()).to.not.eql('');",
                      "});",
                      "pm.test('TC-M02-308-B / Administrador: la peticion es rechazada, no se crea la transferencia', () => pm.expect(code).to.not.be.within(200, 299));",
                      "pm.test('TC-M02-308-B / Administrador: el actor autorizado no recibe 403', () => pm.expect(code).to.not.eql(403));",
                      "pm.test('TC-M02-308-B / Administrador: no se produce un error de servidor', () => pm.expect(code).to.be.below(500));",
                      "pm.test('TC-M02-308-B / Administrador: el activo base 279 esta ACTIVO y su origen vigente es 48', () => pm.expect(Number(pm.collectionVariables.get('origen_279'))).to.eql(48));",
                      "pm.test('TC-M02-308-B / Administrador: el destino 50 es distinto del origen', () => pm.expect(enviado.infraestructura_destino_id).to.not.eql(48));",
                      "pm.test('TC-M02-308-B / Administrador: HTTP 422 segun la ficha (E-05)', () => pm.expect(code).to.eql(422));",
                      "pm.test('TC-M02-308-B / Administrador: el error corresponde a E-05 (INFRAESTRUCTURA_DESTINO_INVALIDA)', () => pm.expect(b.error_code).to.eql('INFRAESTRUCTURA_DESTINO_INVALIDA'));",
                      "pm.test('TC-M02-308-B / Administrador: el mensaje corresponde a destino no activo', () => {",
                      "  pm.expect(txt).to.match(/no existe|no esta activa/);",
                      "  pm.expect(txt).to.include('50');",
                      "});",
                      "pm.test('TC-M02-308-B / Administrador: el rechazo no se debe a otra regla de RF-48', () => { ['activo_no_encontrado', 'sin_infraestructura_origen', 'infraestructura_origen_incorrecta', 'destino_igual_origen', 'incompatibilidad_especie', 'capacidad_excedida', 'activo_no_activo', 'transferencia_concurrente'].forEach((otro) => { pm.expect(txt, 'rechazo atribuido a ' + otro).to.not.include(otro); }); });",
                      "pm.collectionVariables.set('http_admin_308b', code);",
                      "pm.collectionVariables.set('body_admin_308b', pm.response.text().slice(0, 600));"
                    ]
                  }
                }
              ]
            },
            {
              "name": "Verificacion sin cambios tras TC-M02-308-B - Administrador",
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Authorization",
                    "value": "Bearer {{token_admin}}"
                  }
                ],
                "url": "https://sigab-backendtest-389pcb-a48238-158-69-200-27.sslip.io/api-sgpmp-test/activos-biologicos/279"
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "const norm = (s) => String(s).normalize('NFD').replace(/[\\u0300-\\u036f]/g, '').toLowerCase();",
                      "const cuerpo = () => { try { return JSON.stringify(pm.response.json()); } catch (e) { return pm.response.text(); } };",
                      "pm.test('Activo legible', () => pm.response.to.have.status(200));",
                      "const b = pm.response.json();",
                      "pm.test('TC-M02-308-B / Administrador: el activo 279 sigue ACTIVO', () => pm.expect(b.nombre_estado).to.eql('ACTIVO'));",
                      "pm.test('TC-M02-308-B / Administrador: la ubicacion del activo 279 no cambio (48)', () => pm.expect(b.id_infraestructura).to.eql(48));"
                    ]
                  }
                }
              ]
            }
          ]
        }
      ]
    },
    {
      "name": "02-DIAGNOSTICO",
      "item": []
    }
  ],
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "// Fecha de hoy: el DTO rechaza cualquier fecha posterior a la actual (E-10), que queda",
          "// fuera del alcance de este caso.",
          "pm.collectionVariables.set('fecha_hoy', new Date().toISOString().slice(0, 10));"
        ]
      }
    }
  ],
  "variable": [
    {
      "key": "password",
      "value": "Test1234!"
    }
  ]
}