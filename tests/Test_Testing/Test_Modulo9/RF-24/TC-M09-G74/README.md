# TC-M09-G74 — RF-24

Prueba API controlada para `TC-M09-141`: una calibración real con el Ingeniero
de Campo, validación de dispositivo–sensor–área, rango técnico, persistencia e
historial. El runner recibe la contraseña exclusivamente desde la variable de
proceso `TEST_FIELD_ENGINEER_PASSWORD`; no la guarda en archivos.
